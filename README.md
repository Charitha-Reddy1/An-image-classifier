# An-image-classifier
This project is a web-based AI Image Classifier that allows users to upload an image and automatically identifies what the image contains using a pre-trained deep learning model. It is built using Streamlit for the frontend and MobileNetV2 for image classification using Convolutional Neural Network.Python ,TensorFlow Streamlit (UI) OpenCV  &amp; NumPy.
